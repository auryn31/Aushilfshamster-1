name: corona_karma
description: A new Flutter project.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  google_maps_flutter: ^0.5.25
  location: ^2.5.0
  timeago: ^2.0.26
  flutter_svg: ^0.17.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  firebase_core: ^0.4.4+3
  firebase_database: ^3.1.3
  firebase_auth: ^0.15.5+3
  cloud_firestore: ^0.13.4+2
  provider: ^4.0.4

flutter:
  assets:
    - assets/toilet_paper.png
    - assets/can.png
    - assets/corn.png
    - assets/bleach.png
    - assets/spaghetti.png
    - assets/profile.png
    - assets/chatIcons/dog.svg
    - assets/chatIcons/groceries.svg
    - assets/chatIcons/pills.svg
    - assets/chatIcons/postal.svg
    - assets/tabIcons/chat.svg
    - assets/tabIcons/map.svg
    - assets/tabIcons/profile.svg
    - assets/tabIcons/chat_active.svg
    - assets/tabIcons/map_active.svg
    - assets/tabIcons/profile_active.svg
    - assets/maps_position.svg
